# Nepal Job Market Trend Analyzer
# Advanced Python Programming for Data Science — Project
#
# Install inside your .venv:
#   python -m venv .venv
#   .venv\Scripts\activate       (Windows)
#   pip install -r requirements.txt
#
# Using >= pins so pip picks a pre-built wheel for your Python version.
# Exact == pins caused source-build failures on Python 3.13 (no C compiler needed).

# ── Web Scraping (Chapter 2) ───────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── Data Storage ──────────────────────────────────────────────
# sqlite3 is part of Python's standard library (no install needed)

# ── Data Wrangling & ETL (Chapter 3) ──────────────────────────
pandas>=2.2.3             # 2.2.3+ has pre-built wheels for Python 3.13
numpy>=2.0.0              # 2.0+ has pre-built wheels for Python 3.13
openpyxl>=3.1.2           # Excel report generation (Chapter 4) — pure Python

# ── Statistical Analysis (Chapter 6) ──────────────────────────
scipy>=1.14.0             # 1.14+ has Python 3.13 wheels
statsmodels>=0.14.2       # OLS regression — pure Python

# ── Data Visualization (Chapter 5) ────────────────────────────
matplotlib>=3.9.0         # 3.9+ has Python 3.13 wheels
seaborn>=0.13.2           # pure Python — any recent version works
plotly>=5.22.0            # interactive HTML charts (dashboard)

# ── Dashboard (Chapter 5) ─────────────────────────────────────
streamlit>=1.34.0

# ── Pipeline Automation (Chapter 4) ───────────────────────────
schedule>=1.2.1           # CRON-style job scheduling — pure Python
